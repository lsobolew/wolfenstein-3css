# Build intermediates: the map description handed from the extractor to the
# generator. Regenerated by `npm run extract`.
.cache/

# The original game data. Never commit these — they are id Software's.
data/
*.WL1
*.WL6
*.SOD
*.SDM

node_modules/
.DS_Store
